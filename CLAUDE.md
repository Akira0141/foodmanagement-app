# CLAUDE - プロジェクト貢献ガイドライン

このファイルは本プロジェクトに参加する開発者向けのルールと運用指針をまとめたものです。特に Git 運用ルールについては必ず守ってください。

---

## 目的

- プロジェクトに対する貢献（コード・ドキュメント・設定変更など）を円滑かつ安全に行うための基本ルールを定めます。
- コードの品質と履歴の整合性を保ち、共同開発を円滑にすることを目指します。

---

## 基本ルール

- 誠実に変更内容を記述し、他の開発者が理解できる形で作業してください。
- セキュリティやプライバシーに関わる情報（秘密鍵、パスワード、トークン等）はリポジトリに含めないでください。

---

## Git運用ルール（必須）

1. 変更ごとにコミット
   - 1つの論理的な変更は1つのコミットにまとめる。コミットは小さく、説明的に。

2. 変更を行ったら必ず GitHub にプッシュする（重要）
   - ローカルでコミットした変更は、作業が完了した／途中でも、必ず対応するブランチを作成し GitHub にプッシュしてください。
   - 小さな修正（typo や軽微なリファクタ）でも、ローカルに放置せずプッシュすること。
   - プッシュすることで自動バックアップ、CI 実行、チームによる早期レビューが可能になります。

3. ブランチ戦略
   - ブランチ命名例:
     - feature/xxx — 新機能
     - fix/xxx — バグ修正
     - chore/xxx — 雑務（依存更新など）
     - docs/xxx — ドキュメント
   - main（または master）は常にリリース可能な状態を維持する。

4. プルリクエスト（PR）
   - main へ直接プッシュせず、必ず PR を作成してレビューを得てからマージすること。
   - PR には以下を記載すること:
     - 変更の概要
     - 関連する Issue / チケット番号（あれば）
     - テスト手順（再現・確認方法）
     - 互換性やリスクのある変更点

5. CI とテスト
   - PR を作成すると自動で CI が走る設定がある場合、CI が成功するまでマージしないこと。
   - 新しい機能やバグ修正には、可能な限り自動テストを追加する。

6. コミットメッセージ規約（推奨）
   - フォーマット例: <タイプ>(<範囲>): 短い説明
     - 例: feat(auth): add login endpoint
   - タイプ例: feat, fix, docs, style, refactor, chore, test

7. リベース／マージの運用
   - ブランチを最新の main に追従させる場合、リベースを使って履歴を整えることを推奨（チーム合意に従う）。
   - コンフリクト解消後は必ずローカルで動作確認してから再プッシュする。

8. 大容量ファイル
   - 大容量ファイルは Git LFS を使うか、別のストレージに配置する。リポジトリにバイナリを直接追加しないこと。

---

## レビューと責任

- レビューを行う際は、変更の目的・影響範囲・テスト方法を確認すること。
- レビューで問題が見つかった場合は、指摘内容が解消されるまでマージしないこと。

---

## 例外・緊急対応

- セキュリティインシデントや重大なバグにより即時対応が必要な場合は、事前承認なしで main に直接コミット／プッシュすることが許容される。ただし、その後できるだけ早く PR を作成し、変更内容を説明してレビューを受けること。

---

## 付録: 推奨ワークフロー（簡易）

1. ローカルで新しいブランチを作成: git checkout -b feature/your-feature
2. 作業し、機能ごとにコミット: git add . && git commit -m "feat(x): 〜〜"
3. ブランチを GitHub にプッシュ: git push origin feature/your-feature
4. GitHub 上で PR を作成 → レビューと CI を待つ
5. レビューOK & CI通過後に main へマージ

---

このファイルはプロジェクト運用にとって重要なルールをまとめたものです。必要に応じてチームで更新してください。
